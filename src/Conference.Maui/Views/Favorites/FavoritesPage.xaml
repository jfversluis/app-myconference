<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:Conference.Maui.ViewModels"
             xmlns:models="clr-namespace:Conference.Maui.Models"
             x:Class="Conference.Maui.Views.Favorites.FavoritesPage"
             x:DataType="vm:FavoritesViewModel"
             Title="{Binding Title}">

    <Grid>
        <!-- Empty State -->
        <VerticalStackLayout IsVisible="{Binding HasFavorites, Converter={StaticResource InvertedBoolConverter}}"
                           VerticalOptions="Center"
                           HorizontalOptions="Center"
                           Spacing="20"
                           Padding="40">
            <Label Text="⭐"
                   FontSize="72"
                   HorizontalTextAlignment="Center"/>
            <Label Text="No Favorites Yet"
                   FontSize="24"
                   FontAttributes="Bold"
                   HorizontalTextAlignment="Center"/>
            <Label Text="Tap the star icon on any session to add it to your favorites"
                   FontSize="16"
                   HorizontalTextAlignment="Center"
                   TextColor="{AppThemeBinding Light={StaticResource Gray600}, Dark={StaticResource Gray400}}"/>
        </VerticalStackLayout>

        <!-- Favorites List -->
        <RefreshView IsRefreshing="{Binding IsRefreshing}"
                     Command="{Binding RefreshCommand}"
                     IsVisible="{Binding HasFavorites}">
            <CollectionView ItemsSource="{Binding GroupedFavorites}"
                           IsGrouped="False"
                           SelectionMode="None">
                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="models:TimeSlot">
                        <VerticalStackLayout Spacing="0">
                            <!-- Sticky Header with Conflict Warning -->
                            <Grid BackgroundColor="{AppThemeBinding Light={StaticResource Gray200}, Dark={StaticResource Gray800}}"
                                  Padding="15,10">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <Label Grid.Column="0"
                                       Text="{Binding SlotStart}"
                                       FontSize="16"
                                       FontAttributes="Bold"
                                       VerticalOptions="Center"/>

                                <!-- Conflict Warning -->
                                <Border Grid.Column="1"
                                       BackgroundColor="{AppThemeBinding Light={StaticResource Warning}, Dark={StaticResource Warning}}"
                                       Padding="10,5"
                                       IsVisible="{Binding Sessions.Count, Converter={{StaticResource CompareConverter} ComparisonOperator=Greater, ComparingValue=1, TrueObject=True, FalseObject=False}}">
                                    <Border.StrokeShape>
                                        <RoundRectangle CornerRadius="5"/>
                                    </Border.StrokeShape>
                                    <Label Text="⚠️ Conflict"
                                           FontSize="12"
                                           TextColor="{AppThemeBinding Light={StaticResource White}, Dark={StaticResource White}}"
                                           FontAttributes="Bold"/>
                                </Border>
                            </Grid>

                            <!-- Sessions in this slot -->
                            <CollectionView ItemsSource="{Binding Sessions}"
                                          SelectionMode="None">
                                <CollectionView.ItemTemplate>
                                    <DataTemplate x:DataType="models:Session">
                                        <Border Margin="10,5"
                                                Padding="15"
                                                BackgroundColor="{AppThemeBinding Light={StaticResource White}, Dark={StaticResource Gray900}}"
                                                StrokeThickness="1"
                                                Stroke="{AppThemeBinding Light={StaticResource Gray200}, Dark={StaticResource Gray700}}">
                                            <Border.StrokeShape>
                                                <RoundRectangle CornerRadius="12"/>
                                            </Border.StrokeShape>
                                            <Border.GestureRecognizers>
                                                <TapGestureRecognizer Command="{Binding Source={RelativeSource AncestorType={x:Type vm:FavoritesViewModel}}, Path=SessionTappedCommand}"
                                                                     CommandParameter="{Binding .}"/>
                                            </Border.GestureRecognizers>

                                            <Grid RowDefinitions="Auto,Auto,Auto" ColumnDefinitions="*,Auto">
                                                <!-- Title -->
                                                <Label Grid.Row="0" Grid.Column="0"
                                                       Text="{Binding Title}"
                                                       FontSize="16"
                                                       FontAttributes="Bold"
                                                       MaxLines="2"
                                                       LineBreakMode="TailTruncation"/>

                                                <!-- Remove Favorite Button -->
                                                <ImageButton Grid.Row="0" Grid.Column="1"
                                                           Source="close.png"
                                                           WidthRequest="24"
                                                           HeightRequest="24"
                                                           VerticalOptions="Start"
                                                           Command="{Binding Source={RelativeSource AncestorType={x:Type vm:FavoritesViewModel}}, Path=RemoveFavoriteCommand}"
                                                           CommandParameter="{Binding .}"/>

                                                <!-- Room and Time -->
                                                <HorizontalStackLayout Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2"
                                                                      Spacing="10"
                                                                      Margin="0,5,0,0">
                                                    <Label Text="{Binding Room}"
                                                           FontSize="14"
                                                           TextColor="{AppThemeBinding Light={StaticResource Gray600}, Dark={StaticResource Gray400}}"/>
                                                    <Label Text="•"/>
                                                    <Label FontSize="14"
                                                           TextColor="{AppThemeBinding Light={StaticResource Gray600}, Dark={StaticResource Gray400}}">
                                                        <Label.Text>
                                                            <MultiBinding StringFormat="{}{0:HH:mm} - {1:HH:mm}">
                                                                <Binding Path="StartsAt"/>
                                                                <Binding Path="EndsAt"/>
                                                            </MultiBinding>
                                                        </Label.Text>
                                                    </Label>
                                                </HorizontalStackLayout>

                                                <!-- Speakers -->
                                                <Label Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2"
                                                       Text="{Binding Speakers, Converter={StaticResource SpeakersToStringConverter}}"
                                                       FontSize="14"
                                                       Margin="0,5,0,0"
                                                       TextColor="{AppThemeBinding Light={StaticResource Gray600}, Dark={StaticResource Gray400}}"
                                                       IsVisible="{Binding Speakers.Count, Converter={{StaticResource CompareConverter} ComparisonOperator=Greater, ComparingValue=0, TrueObject=True, FalseObject=False}}"/>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </CollectionView.ItemTemplate>
                            </CollectionView>
                        </VerticalStackLayout>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </RefreshView>

        <!-- Loading Indicator -->
        <ActivityIndicator IsRunning="{Binding IsBusy}"
                          IsVisible="{Binding IsBusy}"
                          VerticalOptions="Center"
                          HorizontalOptions="Center"/>
    </Grid>
</ContentPage>